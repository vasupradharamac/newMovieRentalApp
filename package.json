{
  "name": "movie-rental-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "rund": "nodemon server.js",
    "heroku-postbuild": "cd client-app/rental-store && npm install && npm run build",
    "client":"cd client-app/rental-store && npm start",
      "build-client":"cd client-app/rental-store && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "cors": "^2.8.5",
    "diskdb": "^0.1.17",
    "express": "^4.18.1",
    "g": "^2.0.1",
    "mongodb": "^4.9.1",
    "nodemon": "^2.0.19",
    "react-notifications": "^1.7.4"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "nodemonConfig": {
    "ignore": [
      "./data/*",
      "./client-app/*"
    ]
  }
}
